name: harrypotter_magic_spells # 🔄 새 프로젝트 생성 시 수정 (프로젝트 코드명)
description: 음성으로 마법 주문을 외치는 해리포터 테마 앱 # 🔄 새 프로젝트 생성 시 수정

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1 # 🔄 새 프로젝트 생성 시 수정 (1.0.0+1로 시작)

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  torch_light: ^1.1.0
  speech_to_text: ^7.0.0
  audioplayers: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
    - assets/image/